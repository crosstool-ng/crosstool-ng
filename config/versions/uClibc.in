#
# DO NOT EDIT! This file is automatically generated.
#

# The component directory name
config UCLIBC_DIR_NAME
    string
    default "uClibc"


choice
    bool "Show uClibc versions from"

config UCLIBC_USE_UCLIBC_NG_ORG
    bool "uclibc-ng.org"
    help
      uClibc from uclibc-ng.org.

config UCLIBC_USE_UCLIBC_ORG
    bool "uclibc.org"
    depends on OBSOLETE
    help
      uClibc from uclibc.org.

endchoice

config UCLIBC_USE
    string
    default "UCLIBC_NG" if UCLIBC_USE_UCLIBC_NG_ORG
    default "UCLIBC" if UCLIBC_USE_UCLIBC_ORG


if UCLIBC_USE_UCLIBC_NG_ORG

config UCLIBC_NG_PKG_NAME
    string
    default "uClibc-ng"

choice
    bool "Source of uClibc-ng"

config UCLIBC_NG_SRC_RELEASE
    bool "Released tarball"
    help
      Download a released tarball.

config UCLIBC_NG_SRC_DEVEL
    bool "Vendor/custom repository"
    depends on EXPERIMENTAL
    help
      Check out from a repository.
      Default is the vendor repository at git://uclibc-ng.org/git/uclibc-ng

if UCLIBC_NG_SRC_DEVEL

choice
    bool "VCS type"
    default UCLIBC_NG_DEVEL_VCS_git
    help
      Version control system from which the sources will be checked out.
      The default value points to the development repository for uClibc-ng.

config UCLIBC_NG_DEVEL_VCS_git
    bool "Git"

config UCLIBC_NG_DEVEL_VCS_svn
    bool "Subversion"

config UCLIBC_NG_DEVEL_VCS_hg
    bool "Mercurial"

config UCLIBC_NG_DEVEL_VCS_cvs
    bool "CVS"

endchoice

config UCLIBC_NG_DEVEL_VCS
    string
    default "git" if UCLIBC_NG_DEVEL_VCS_git
    default "svn" if UCLIBC_NG_DEVEL_VCS_svn
    default "hg" if UCLIBC_NG_DEVEL_VCS_hg
    default "cvs" if UCLIBC_NG_DEVEL_VCS_cvs

config UCLIBC_NG_DEVEL_URL
    string "Repository URL"
    default "git://uclibc-ng.org/git/uclibc-ng"
    help
      Repository URL.

      For CVS, enter both the value of CVS root and the module name, separated
      by a space.

config UCLIBC_NG_DEVEL_BRANCH
    string "Branch/tag to check out"
    default ""
    help
      Git/CVS: branch/tag to be checked out
      Subversion: directories to append to the repository URL (i.e. branch or tag)
      Mercurial: official guide recommends using separate repositories to maintain
      stable branches. You likely need to change the repository URL, rather than
      enter anything here.

config UCLIBC_NG_DEVEL_REVISION
    string "Revision/changeset"
    default ""
    help
      Commit ID or revision ID to check out.
      Git: enter the commit ID to check out a commit.
      CVS: enter the date in "YYYY/MM/DD HH:MM:SS" format (UTC) to check out certain date.
      Subversion: enter the revision.

config UCLIBC_NG_DEVEL_SUBDIR
    string "Subdirectory in the repository"
    default ""
    help
      Some projects produce releases not from the top-level directory in the
      repository, but rather from some subdirectory. If it is the case,
      specify this subdirectory here.

config UCLIBC_NG_DEVEL_BOOTSTRAP
    string "Bootstrap command"
    default ""
    help
      Command to run after checking out. Some projects don't store the generated
      files like configure script in the repository; building out of a checked out
      working copy thus requires some extra steps. Separate multiple shell commands
      with &&.

endif

config UCLIBC_NG_SRC_CUSTOM
    bool "Custom location"
    depends on EXPERIMENTAL
    help
      Custom directory or tarball.

if UCLIBC_NG_SRC_CUSTOM

config UCLIBC_NG_CUSTOM_LOCATION
    string "Custom source location"
    help
      Path to the directory or tarball with the sources.

endif

endchoice

choice
    bool "Version of uClibc-ng"
    help
      For a released version, select the version of uClibc-ng to download
      and build. For sources out of the vendor repository or from a custom
      location, select the version that describes those custom sources.
      Based on this version, crosstool-NG may apply some version-specific
      quirks while building uClibc-ng.

config UCLIBC_NG_VERY_NEW
    bool "newer than anything below"
    depends on EXPERIMENTAL
    depends on UCLIBC_NG_SRC_DEVEL || UCLIBC_NG_SRC_CUSTOM
    select UCLIBC_1_0_15_or_later
    depends on !UCLIBC_REQUIRE_1_0_15_or_older
    select UCLIBC_1_0_0_or_later
    depends on !UCLIBC_REQUIRE_1_0_0_or_older
    select UCLIBC_0_9_33_2_or_later
    depends on !UCLIBC_REQUIRE_0_9_33_2_or_older

config UCLIBC_NG_V_1_0_27
    bool "1.0.27"
    select UCLIBC_1_0_15_or_later
    depends on !UCLIBC_REQUIRE_1_0_15_or_older
    select UCLIBC_1_0_0_or_later
    depends on !UCLIBC_REQUIRE_1_0_0_or_older
    select UCLIBC_0_9_33_2_or_later
    depends on !UCLIBC_REQUIRE_0_9_33_2_or_older

config UCLIBC_NG_V_1_0_26
    bool "1.0.26"
    select UCLIBC_1_0_15_or_later
    depends on !UCLIBC_REQUIRE_1_0_15_or_older
    select UCLIBC_1_0_0_or_later
    depends on !UCLIBC_REQUIRE_1_0_0_or_older
    select UCLIBC_0_9_33_2_or_later
    depends on !UCLIBC_REQUIRE_0_9_33_2_or_older

config UCLIBC_NG_V_1_0_25
    bool "1.0.25"
    select UCLIBC_1_0_15_or_later
    depends on !UCLIBC_REQUIRE_1_0_15_or_older
    select UCLIBC_1_0_0_or_later
    depends on !UCLIBC_REQUIRE_1_0_0_or_older
    select UCLIBC_0_9_33_2_or_later
    depends on !UCLIBC_REQUIRE_0_9_33_2_or_older

config UCLIBC_NG_VERY_OLD
    bool "older than anything above"
    depends on OBSOLETE && EXPERIMENTAL
    depends on UCLIBC_NG_SRC_DEVEL || UCLIBC_NG_SRC_CUSTOM
    depends on !UCLIBC_REQUIRE_1_0_15_or_later
    depends on !UCLIBC_REQUIRE_1_0_0_or_later
    depends on !UCLIBC_REQUIRE_0_9_33_2_or_later

endchoice


config UCLIBC_NG_VERSION
    string
    default "1.0.27" if UCLIBC_NG_V_1_0_27
    default "1.0.26" if UCLIBC_NG_V_1_0_26
    default "1.0.25" if UCLIBC_NG_V_1_0_25
    default "new" if UCLIBC_NG_VERY_NEW
    default "old" if UCLIBC_NG_VERY_OLD
    default "unknown"

config UCLIBC_NG_MIRRORS
    string
    default "http://downloads.uclibc-ng.org/releases/${CT_UCLIBC_NG_VERSION}"

config UCLIBC_NG_ARCHIVE_FILENAME
    string
    default "@{pkg_name}-@{version}"

config UCLIBC_NG_ARCHIVE_DIRNAME
    string
    default "@{pkg_name}-@{version}"

config UCLIBC_NG_ARCHIVE_FORMATS
    string
    default ".tar.xz .tar.lz .tar.bz2 .tar.gz"

config UCLIBC_NG_SIGNATURE_FORMAT
    string
    default "packed/.asc"


endif

if UCLIBC_USE_UCLIBC_ORG

config UCLIBC_PKG_NAME
    string
    default "uClibc"

choice
    bool "Source of uClibc"

config UCLIBC_SRC_RELEASE
    bool "Released tarball"
    help
      Download a released tarball.

config UCLIBC_SRC_DEVEL
    bool "Vendor/custom repository"
    depends on EXPERIMENTAL
    help
      Check out from a repository.
      Default is the vendor repository at git://git.busybox.net/uClibc

if UCLIBC_SRC_DEVEL

choice
    bool "VCS type"
    default UCLIBC_DEVEL_VCS_git
    help
      Version control system from which the sources will be checked out.
      The default value points to the development repository for uClibc.

config UCLIBC_DEVEL_VCS_git
    bool "Git"

config UCLIBC_DEVEL_VCS_svn
    bool "Subversion"

config UCLIBC_DEVEL_VCS_hg
    bool "Mercurial"

config UCLIBC_DEVEL_VCS_cvs
    bool "CVS"

endchoice

config UCLIBC_DEVEL_VCS
    string
    default "git" if UCLIBC_DEVEL_VCS_git
    default "svn" if UCLIBC_DEVEL_VCS_svn
    default "hg" if UCLIBC_DEVEL_VCS_hg
    default "cvs" if UCLIBC_DEVEL_VCS_cvs

config UCLIBC_DEVEL_URL
    string "Repository URL"
    default "git://git.busybox.net/uClibc"
    help
      Repository URL.

      For CVS, enter both the value of CVS root and the module name, separated
      by a space.

config UCLIBC_DEVEL_BRANCH
    string "Branch/tag to check out"
    default ""
    help
      Git/CVS: branch/tag to be checked out
      Subversion: directories to append to the repository URL (i.e. branch or tag)
      Mercurial: official guide recommends using separate repositories to maintain
      stable branches. You likely need to change the repository URL, rather than
      enter anything here.

config UCLIBC_DEVEL_REVISION
    string "Revision/changeset"
    default ""
    help
      Commit ID or revision ID to check out.
      Git: enter the commit ID to check out a commit.
      CVS: enter the date in "YYYY/MM/DD HH:MM:SS" format (UTC) to check out certain date.
      Subversion: enter the revision.

config UCLIBC_DEVEL_SUBDIR
    string "Subdirectory in the repository"
    default ""
    help
      Some projects produce releases not from the top-level directory in the
      repository, but rather from some subdirectory. If it is the case,
      specify this subdirectory here.

config UCLIBC_DEVEL_BOOTSTRAP
    string "Bootstrap command"
    default ""
    help
      Command to run after checking out. Some projects don't store the generated
      files like configure script in the repository; building out of a checked out
      working copy thus requires some extra steps. Separate multiple shell commands
      with &&.

endif

config UCLIBC_SRC_CUSTOM
    bool "Custom location"
    depends on EXPERIMENTAL
    help
      Custom directory or tarball.

if UCLIBC_SRC_CUSTOM

config UCLIBC_CUSTOM_LOCATION
    string "Custom source location"
    help
      Path to the directory or tarball with the sources.

endif

endchoice

choice
    bool "Version of uClibc"
    help
      For a released version, select the version of uClibc to download
      and build. For sources out of the vendor repository or from a custom
      location, select the version that describes those custom sources.
      Based on this version, crosstool-NG may apply some version-specific
      quirks while building uClibc.

config UCLIBC_VERY_NEW
    bool "newer than anything below"
    depends on EXPERIMENTAL
    depends on UCLIBC_SRC_DEVEL || UCLIBC_SRC_CUSTOM
    select UCLIBC_1_0_15_or_later
    depends on !UCLIBC_REQUIRE_1_0_15_or_older
    select UCLIBC_1_0_0_or_later
    depends on !UCLIBC_REQUIRE_1_0_0_or_older
    select UCLIBC_0_9_33_2_or_later
    depends on !UCLIBC_REQUIRE_0_9_33_2_or_older

config UCLIBC_V_0_9_33_2
    bool "0.9.33.2 (OBSOLETE)"
    depends on OBSOLETE
    select UCLIBC_1_0_15_or_older
    depends on !UCLIBC_REQUIRE_1_0_15_or_later
    select UCLIBC_1_0_0_or_older
    depends on !UCLIBC_REQUIRE_1_0_0_or_later
    select UCLIBC_0_9_33_2_or_later
    select UCLIBC_0_9_33_2_or_older

config UCLIBC_VERY_OLD
    bool "older than anything above"
    depends on OBSOLETE && EXPERIMENTAL
    depends on UCLIBC_SRC_DEVEL || UCLIBC_SRC_CUSTOM
    depends on !UCLIBC_REQUIRE_1_0_15_or_later
    depends on !UCLIBC_REQUIRE_1_0_0_or_later
    depends on !UCLIBC_REQUIRE_0_9_33_2_or_later

endchoice


config UCLIBC_VERSION
    string
    default "0.9.33.2" if UCLIBC_V_0_9_33_2
    default "new" if UCLIBC_VERY_NEW
    default "old" if UCLIBC_VERY_OLD
    default "unknown"

config UCLIBC_MIRRORS
    string
    default "http://www.uclibc.org/downloads http://www.uclibc.org/downloads/old-releases"

config UCLIBC_ARCHIVE_FILENAME
    string
    default "@{pkg_name}-@{version}"

config UCLIBC_ARCHIVE_DIRNAME
    string
    default "@{pkg_name}-@{version}"

config UCLIBC_ARCHIVE_FORMATS
    string
    default ".tar.xz .tar.bz2"

config UCLIBC_SIGNATURE_FORMAT
    string
    default ""


endif


config UCLIBC_1_0_15_or_later
    bool

config UCLIBC_1_0_15_or_older
    bool

config UCLIBC_REQUIRE_1_0_15_or_later
    bool

config UCLIBC_REQUIRE_1_0_15_or_older
    bool

config UCLIBC_1_0_0_or_later
    bool

config UCLIBC_1_0_0_or_older
    bool

config UCLIBC_REQUIRE_1_0_0_or_later
    bool

config UCLIBC_REQUIRE_1_0_0_or_older
    bool

config UCLIBC_0_9_33_2_or_later
    bool

config UCLIBC_0_9_33_2_or_older
    bool

config UCLIBC_REQUIRE_0_9_33_2_or_later
    bool

config UCLIBC_REQUIRE_0_9_33_2_or_older
    bool

