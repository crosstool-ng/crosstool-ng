#
# DO NOT EDIT! This file is automatically generated.
#

# The component directory name
config GCC_DIR_NAME
    string
    default "gcc"


choice
    bool "Show gcc versions from"

config GCC_USE_GNU
    bool "GNU"
    help
      GNU (which is a recursive acronym for "GNU's Not Unix") provides GNU binutils,
      GNU C Compiler (gcc), GNU debugger (gdb) and many other utilities. GNU is
      considered the master source for these packages.
      
      You should select "GNU" here unless you definitely know that you need some version
      from another source.

config GCC_USE_LINARO
    bool "Linaro"
    depends on EXPERIMENTAL
    help
      Linaro is maintaining some advanced/more stable/experimental versions
      of binutils, gcc, glibc and gdb, especially for the ARM architecture.
      These versions contain some changes that are not (yet?) merged into
      their respective upstream repositories.

endchoice

config GCC_USE
    string
    default "GCC" if GCC_USE_GNU
    default "GCC_LINARO" if GCC_USE_LINARO


if GCC_USE_GNU

config GCC_PKG_NAME
    string
    default "gcc"

choice
    bool "Source of gcc"

config GCC_SRC_RELEASE
    bool "Released tarball"
    help
      Download a released tarball.

config GCC_SRC_DEVEL
    bool "Vendor/custom repository"
    depends on EXPERIMENTAL
    help
      Check out from a repository.
      Default is the vendor repository at svn://gcc.gnu.org/svn/gcc

if GCC_SRC_DEVEL

choice
    bool "VCS type"
    default GCC_DEVEL_VCS_svn
    help
      Version control system from which the sources will be checked out.
      The default value points to the development repository for gcc.

config GCC_DEVEL_VCS_git
    bool "Git"

config GCC_DEVEL_VCS_svn
    bool "Subversion"

config GCC_DEVEL_VCS_hg
    bool "Mercurial"

config GCC_DEVEL_VCS_cvs
    bool "CVS"

endchoice

config GCC_DEVEL_VCS
    string
    default "git" if GCC_DEVEL_VCS_git
    default "svn" if GCC_DEVEL_VCS_svn
    default "hg" if GCC_DEVEL_VCS_hg
    default "cvs" if GCC_DEVEL_VCS_cvs

config GCC_DEVEL_URL
    string "Repository URL"
    default "svn://gcc.gnu.org/svn/gcc"
    help
      Repository URL.

      For CVS, enter both the value of CVS root and the module name, separated
      by a space.

config GCC_DEVEL_BRANCH
    string "Branch/tag to check out"
    default ""
    help
      Git/CVS: branch/tag to be checked out
      Subversion: directories to append to the repository URL (i.e. branch or tag)
      Mercurial: official guide recommends using separate repositories to maintain
      stable branches. You likely need to change the repository URL, rather than
      enter anything here.

config GCC_DEVEL_REVISION
    string "Revision/changeset"
    default ""
    help
      Commit ID or revision ID to check out.
      Git: enter the commit ID to check out a commit.
      CVS: enter the date in "YYYY/MM/DD HH:MM:SS" format (UTC) to check out certain date.
      Subversion: enter the revision.

config GCC_DEVEL_SUBDIR
    string "Subdirectory in the repository"
    default ""
    help
      Some projects produce releases not from the top-level directory in the
      repository, but rather from some subdirectory. If it is the case,
      specify this subdirectory here.

config GCC_DEVEL_BOOTSTRAP
    string "Bootstrap command"
    default ""
    help
      Command to run after checking out. Some projects don't store the generated
      files like configure script in the repository; building out of a checked out
      working copy thus requires some extra steps. Separate multiple shell commands
      with &&.

endif

config GCC_SRC_CUSTOM
    bool "Custom location"
    depends on EXPERIMENTAL
    help
      Custom directory or tarball.

if GCC_SRC_CUSTOM

config GCC_CUSTOM_LOCATION
    string "Custom source location"
    help
      Path to the directory or tarball with the sources.

endif

endchoice

choice
    bool "Version of gcc"
    help
      For a released version, select the version of gcc to download
      and build. For sources out of the vendor repository or from a custom
      location, select the version that describes those custom sources.
      Based on this version, crosstool-NG may apply some version-specific
      quirks while building gcc.

config GCC_VERY_NEW
    bool "newer than anything below"
    depends on EXPERIMENTAL
    depends on GCC_SRC_DEVEL || GCC_SRC_CUSTOM
    select GCC_7_or_later
    depends on !GCC_REQUIRE_7_or_older
    select GCC_6_or_later
    depends on !GCC_REQUIRE_6_or_older
    select GCC_5_or_later
    depends on !GCC_REQUIRE_5_or_older
    select GCC_4_9_2_or_later
    depends on !GCC_REQUIRE_4_9_2_or_older
    select GCC_4_9_or_later
    depends on !GCC_REQUIRE_4_9_or_older
    select GCC_4_8_or_later
    depends on !GCC_REQUIRE_4_8_or_older

config GCC_V_7_2_0
    bool "7.2.0"
    select GCC_7_or_later
    depends on !GCC_REQUIRE_7_or_older
    select GCC_6_or_later
    depends on !GCC_REQUIRE_6_or_older
    select GCC_5_or_later
    depends on !GCC_REQUIRE_5_or_older
    select GCC_4_9_2_or_later
    depends on !GCC_REQUIRE_4_9_2_or_older
    select GCC_4_9_or_later
    depends on !GCC_REQUIRE_4_9_or_older
    select GCC_4_8_or_later
    depends on !GCC_REQUIRE_4_8_or_older

config GCC_V_6_4_0
    bool "6.4.0"
    select GCC_7_or_older
    depends on !GCC_REQUIRE_7_or_later
    select GCC_6_or_later
    depends on !GCC_REQUIRE_6_or_older
    select GCC_5_or_later
    depends on !GCC_REQUIRE_5_or_older
    select GCC_4_9_2_or_later
    depends on !GCC_REQUIRE_4_9_2_or_older
    select GCC_4_9_or_later
    depends on !GCC_REQUIRE_4_9_or_older
    select GCC_4_8_or_later
    depends on !GCC_REQUIRE_4_8_or_older

config GCC_V_5_5_0
    bool "5.5.0"
    select GCC_7_or_older
    depends on !GCC_REQUIRE_7_or_later
    select GCC_6_or_older
    depends on !GCC_REQUIRE_6_or_later
    select GCC_5_or_later
    depends on !GCC_REQUIRE_5_or_older
    select GCC_4_9_2_or_later
    depends on !GCC_REQUIRE_4_9_2_or_older
    select GCC_4_9_or_later
    depends on !GCC_REQUIRE_4_9_or_older
    select GCC_4_8_or_later
    depends on !GCC_REQUIRE_4_8_or_older

config GCC_V_4_9_4
    bool "4.9.4"
    select GCC_7_or_older
    depends on !GCC_REQUIRE_7_or_later
    select GCC_6_or_older
    depends on !GCC_REQUIRE_6_or_later
    select GCC_5_or_older
    depends on !GCC_REQUIRE_5_or_later
    select GCC_4_9_2_or_later
    depends on !GCC_REQUIRE_4_9_2_or_older
    select GCC_4_9_or_later
    depends on !GCC_REQUIRE_4_9_or_older
    select GCC_4_8_or_later
    depends on !GCC_REQUIRE_4_8_or_older

config GCC_V_4_8_5
    bool "4.8.5 (OBSOLETE)"
    depends on OBSOLETE
    select GCC_7_or_older
    depends on !GCC_REQUIRE_7_or_later
    select GCC_6_or_older
    depends on !GCC_REQUIRE_6_or_later
    select GCC_5_or_older
    depends on !GCC_REQUIRE_5_or_later
    select GCC_4_9_2_or_older
    depends on !GCC_REQUIRE_4_9_2_or_later
    select GCC_4_9_or_older
    depends on !GCC_REQUIRE_4_9_or_later
    select GCC_4_8_or_later
    depends on !GCC_REQUIRE_4_8_or_older

config GCC_VERY_OLD
    bool "older than anything above"
    depends on OBSOLETE && EXPERIMENTAL
    depends on GCC_SRC_DEVEL || GCC_SRC_CUSTOM
    depends on !GCC_REQUIRE_7_or_later
    depends on !GCC_REQUIRE_6_or_later
    depends on !GCC_REQUIRE_5_or_later
    depends on !GCC_REQUIRE_4_9_2_or_later
    depends on !GCC_REQUIRE_4_9_or_later
    depends on !GCC_REQUIRE_4_8_or_later

endchoice


config GCC_VERSION
    string
    default "7.2.0" if GCC_V_7_2_0
    default "6.4.0" if GCC_V_6_4_0
    default "5.5.0" if GCC_V_5_5_0
    default "4.9.4" if GCC_V_4_9_4
    default "4.8.5" if GCC_V_4_8_5
    default "new" if GCC_VERY_NEW
    default "old" if GCC_VERY_OLD
    default "unknown"

config GCC_MIRRORS
    string
    default "$(CT_Mirrors GNU gcc/gcc-${CT_GCC_VERSION}) $(CT_Mirrors sourceware gcc/releases/gcc-${CT_GCC_VERSION})"

config GCC_ARCHIVE_FILENAME
    string
    default "@{pkg_name}-@{version}"

config GCC_ARCHIVE_DIRNAME
    string
    default "@{pkg_name}-@{version}"

config GCC_ARCHIVE_FORMATS
    string
    default ".tar.bz2 .tar.gz" if GCC_V_4_9_4
    default ".tar.bz2 .tar.gz" if GCC_V_4_8_5
    default ".tar.xz .tar.gz"

config GCC_SIGNATURE_FORMAT
    string
    default ""


endif

if GCC_USE_LINARO

config GCC_LINARO_PKG_NAME
    string
    default "gcc-linaro"

choice
    bool "Source of gcc-linaro"

config GCC_LINARO_SRC_RELEASE
    bool "Released tarball"
    help
      Download a released tarball.

config GCC_LINARO_SRC_DEVEL
    bool "Vendor/custom repository"
    depends on EXPERIMENTAL
    help
      Check out from a repository.
      Default is the vendor repository at https://git.linaro.org/toolchain/gcc.git

if GCC_LINARO_SRC_DEVEL

choice
    bool "VCS type"
    default GCC_LINARO_DEVEL_VCS_git
    help
      Version control system from which the sources will be checked out.
      The default value points to the development repository for gcc-linaro.

config GCC_LINARO_DEVEL_VCS_git
    bool "Git"

config GCC_LINARO_DEVEL_VCS_svn
    bool "Subversion"

config GCC_LINARO_DEVEL_VCS_hg
    bool "Mercurial"

config GCC_LINARO_DEVEL_VCS_cvs
    bool "CVS"

endchoice

config GCC_LINARO_DEVEL_VCS
    string
    default "git" if GCC_LINARO_DEVEL_VCS_git
    default "svn" if GCC_LINARO_DEVEL_VCS_svn
    default "hg" if GCC_LINARO_DEVEL_VCS_hg
    default "cvs" if GCC_LINARO_DEVEL_VCS_cvs

config GCC_LINARO_DEVEL_URL
    string "Repository URL"
    default "https://git.linaro.org/toolchain/gcc.git"
    help
      Repository URL.

      For CVS, enter both the value of CVS root and the module name, separated
      by a space.

config GCC_LINARO_DEVEL_BRANCH
    string "Branch/tag to check out"
    default ""
    help
      Git/CVS: branch/tag to be checked out
      Subversion: directories to append to the repository URL (i.e. branch or tag)
      Mercurial: official guide recommends using separate repositories to maintain
      stable branches. You likely need to change the repository URL, rather than
      enter anything here.

config GCC_LINARO_DEVEL_REVISION
    string "Revision/changeset"
    default ""
    help
      Commit ID or revision ID to check out.
      Git: enter the commit ID to check out a commit.
      CVS: enter the date in "YYYY/MM/DD HH:MM:SS" format (UTC) to check out certain date.
      Subversion: enter the revision.

config GCC_LINARO_DEVEL_SUBDIR
    string "Subdirectory in the repository"
    default ""
    help
      Some projects produce releases not from the top-level directory in the
      repository, but rather from some subdirectory. If it is the case,
      specify this subdirectory here.

config GCC_LINARO_DEVEL_BOOTSTRAP
    string "Bootstrap command"
    default ""
    help
      Command to run after checking out. Some projects don't store the generated
      files like configure script in the repository; building out of a checked out
      working copy thus requires some extra steps. Separate multiple shell commands
      with &&.

endif

config GCC_LINARO_SRC_CUSTOM
    bool "Custom location"
    depends on EXPERIMENTAL
    help
      Custom directory or tarball.

if GCC_LINARO_SRC_CUSTOM

config GCC_LINARO_CUSTOM_LOCATION
    string "Custom source location"
    help
      Path to the directory or tarball with the sources.

endif

endchoice

choice
    bool "Version of gcc-linaro"
    help
      For a released version, select the version of gcc-linaro to download
      and build. For sources out of the vendor repository or from a custom
      location, select the version that describes those custom sources.
      Based on this version, crosstool-NG may apply some version-specific
      quirks while building gcc-linaro.

config GCC_LINARO_VERY_NEW
    bool "newer than anything below"
    depends on EXPERIMENTAL
    depends on GCC_LINARO_SRC_DEVEL || GCC_LINARO_SRC_CUSTOM
    select GCC_7_or_later
    depends on !GCC_REQUIRE_7_or_older
    select GCC_6_or_later
    depends on !GCC_REQUIRE_6_or_older
    select GCC_5_or_later
    depends on !GCC_REQUIRE_5_or_older
    select GCC_4_9_2_or_later
    depends on !GCC_REQUIRE_4_9_2_or_older
    select GCC_4_9_or_later
    depends on !GCC_REQUIRE_4_9_or_older
    select GCC_4_8_or_later
    depends on !GCC_REQUIRE_4_8_or_older

config GCC_LINARO_V_7_2_2017_11
    bool "7.2-2017.11 (EXPERIMENTAL)"
    depends on EXPERIMENTAL
    select GCC_7_or_later
    depends on !GCC_REQUIRE_7_or_older
    select GCC_6_or_later
    depends on !GCC_REQUIRE_6_or_older
    select GCC_5_or_later
    depends on !GCC_REQUIRE_5_or_older
    select GCC_4_9_2_or_later
    depends on !GCC_REQUIRE_4_9_2_or_older
    select GCC_4_9_or_later
    depends on !GCC_REQUIRE_4_9_or_older
    select GCC_4_8_or_later
    depends on !GCC_REQUIRE_4_8_or_older

config GCC_LINARO_V_6_4_2017_11
    bool "6.4-2017.11 (EXPERIMENTAL)"
    depends on EXPERIMENTAL
    select GCC_7_or_older
    depends on !GCC_REQUIRE_7_or_later
    select GCC_6_or_later
    depends on !GCC_REQUIRE_6_or_older
    select GCC_5_or_later
    depends on !GCC_REQUIRE_5_or_older
    select GCC_4_9_2_or_later
    depends on !GCC_REQUIRE_4_9_2_or_older
    select GCC_4_9_or_later
    depends on !GCC_REQUIRE_4_9_or_older
    select GCC_4_8_or_later
    depends on !GCC_REQUIRE_4_8_or_older

config GCC_LINARO_V_5_5_2017_10
    bool "5.5-2017.10 (EXPERIMENTAL)"
    depends on EXPERIMENTAL
    select GCC_7_or_older
    depends on !GCC_REQUIRE_7_or_later
    select GCC_6_or_older
    depends on !GCC_REQUIRE_6_or_later
    select GCC_5_or_later
    depends on !GCC_REQUIRE_5_or_older
    select GCC_4_9_2_or_later
    depends on !GCC_REQUIRE_4_9_2_or_older
    select GCC_4_9_or_later
    depends on !GCC_REQUIRE_4_9_or_older
    select GCC_4_8_or_later
    depends on !GCC_REQUIRE_4_8_or_older

config GCC_LINARO_V_4_9_2017_01
    bool "4.9-2017.01 (EXPERIMENTAL)"
    depends on EXPERIMENTAL
    select GCC_7_or_older
    depends on !GCC_REQUIRE_7_or_later
    select GCC_6_or_older
    depends on !GCC_REQUIRE_6_or_later
    select GCC_5_or_older
    depends on !GCC_REQUIRE_5_or_later
    select GCC_4_9_2_or_older
    depends on !GCC_REQUIRE_4_9_2_or_later
    select GCC_4_9_or_later
    depends on !GCC_REQUIRE_4_9_or_older
    select GCC_4_8_or_later
    depends on !GCC_REQUIRE_4_8_or_older

config GCC_LINARO_V_4_8_2015_06
    bool "4.8-2015.06 (OBSOLETE) (EXPERIMENTAL)"
    depends on OBSOLETE
    depends on EXPERIMENTAL
    select GCC_7_or_older
    depends on !GCC_REQUIRE_7_or_later
    select GCC_6_or_older
    depends on !GCC_REQUIRE_6_or_later
    select GCC_5_or_older
    depends on !GCC_REQUIRE_5_or_later
    select GCC_4_9_2_or_older
    depends on !GCC_REQUIRE_4_9_2_or_later
    select GCC_4_9_or_older
    depends on !GCC_REQUIRE_4_9_or_later
    select GCC_4_8_or_later
    depends on !GCC_REQUIRE_4_8_or_older

config GCC_LINARO_VERY_OLD
    bool "older than anything above"
    depends on OBSOLETE && EXPERIMENTAL
    depends on GCC_LINARO_SRC_DEVEL || GCC_LINARO_SRC_CUSTOM
    depends on !GCC_REQUIRE_7_or_later
    depends on !GCC_REQUIRE_6_or_later
    depends on !GCC_REQUIRE_5_or_later
    depends on !GCC_REQUIRE_4_9_2_or_later
    depends on !GCC_REQUIRE_4_9_or_later
    depends on !GCC_REQUIRE_4_8_or_later

endchoice


config GCC_LINARO_VERSION
    string
    default "7.2-2017.11" if GCC_LINARO_V_7_2_2017_11
    default "6.4-2017.11" if GCC_LINARO_V_6_4_2017_11
    default "5.5-2017.10" if GCC_LINARO_V_5_5_2017_10
    default "4.9-2017.01" if GCC_LINARO_V_4_9_2017_01
    default "4.8-2015.06" if GCC_LINARO_V_4_8_2015_06
    default "new" if GCC_LINARO_VERY_NEW
    default "old" if GCC_LINARO_VERY_OLD
    default "unknown"

config GCC_LINARO_MIRRORS
    string
    default "$(CT_Mirrors Linaro gcc ${CT_GCC_LINARO_VERSION})"

config GCC_LINARO_ARCHIVE_FILENAME
    string
    default "@{pkg_name}-@{version}"

config GCC_LINARO_ARCHIVE_DIRNAME
    string
    default "@{pkg_name}-@{version}"

config GCC_LINARO_ARCHIVE_FORMATS
    string
    default ".tar.xz"

config GCC_LINARO_SIGNATURE_FORMAT
    string
    default ""


endif


config GCC_7_or_later
    bool

config GCC_7_or_older
    bool

config GCC_REQUIRE_7_or_later
    bool

config GCC_REQUIRE_7_or_older
    bool

config GCC_6_or_later
    bool

config GCC_6_or_older
    bool

config GCC_REQUIRE_6_or_later
    bool

config GCC_REQUIRE_6_or_older
    bool

config GCC_5_or_later
    bool

config GCC_5_or_older
    bool

config GCC_REQUIRE_5_or_later
    bool

config GCC_REQUIRE_5_or_older
    bool

config GCC_4_9_2_or_later
    bool

config GCC_4_9_2_or_older
    bool

config GCC_REQUIRE_4_9_2_or_later
    bool

config GCC_REQUIRE_4_9_2_or_older
    bool

config GCC_4_9_or_later
    bool

config GCC_4_9_or_older
    bool

config GCC_REQUIRE_4_9_or_later
    bool

config GCC_REQUIRE_4_9_or_older
    bool

config GCC_4_8_or_later
    bool

config GCC_4_8_or_older
    bool

config GCC_REQUIRE_4_8_or_later
    bool

config GCC_REQUIRE_4_8_or_older
    bool

