#
# DO NOT EDIT! This file is automatically generated.
#

# The component directory name
config ANDROID_NDK_DIR_NAME
    string
    default "android-ndk"



config ANDROID_NDK_PKG_NAME
    string
    default "android-ndk"

choice
    bool "Source of android-ndk"

config ANDROID_NDK_SRC_RELEASE
    bool "Released tarball"
    help
      Download a released tarball.

config ANDROID_NDK_SRC_DEVEL
    bool "Vendor/custom repository"
    depends on EXPERIMENTAL
    help
      Check out from a repository.

if ANDROID_NDK_SRC_DEVEL

choice
    bool "VCS type"
    help
      Version control system from which the sources will be checked out.
      The default value points to the development repository for android-ndk.

config ANDROID_NDK_DEVEL_VCS_git
    bool "Git"

config ANDROID_NDK_DEVEL_VCS_svn
    bool "Subversion"

config ANDROID_NDK_DEVEL_VCS_hg
    bool "Mercurial"

config ANDROID_NDK_DEVEL_VCS_cvs
    bool "CVS"

endchoice

config ANDROID_NDK_DEVEL_VCS
    string
    default "git" if ANDROID_NDK_DEVEL_VCS_git
    default "svn" if ANDROID_NDK_DEVEL_VCS_svn
    default "hg" if ANDROID_NDK_DEVEL_VCS_hg
    default "cvs" if ANDROID_NDK_DEVEL_VCS_cvs

config ANDROID_NDK_DEVEL_URL
    string "Repository URL"
    help
      Repository URL.

      For CVS, enter both the value of CVS root and the module name, separated
      by a space.

config ANDROID_NDK_DEVEL_BRANCH
    string "Branch/tag to check out"
    default ""
    help
      Git/CVS: branch/tag to be checked out
      Subversion: directories to append to the repository URL (i.e. branch or tag)
      Mercurial: official guide recommends using separate repositories to maintain
      stable branches. You likely need to change the repository URL, rather than
      enter anything here.

config ANDROID_NDK_DEVEL_REVISION
    string "Revision/changeset"
    default ""
    help
      Commit ID or revision ID to check out.
      Git: enter the commit ID to check out a commit.
      CVS: enter the date in "YYYY/MM/DD HH:MM:SS" format (UTC) to check out certain date.
      Subversion: enter the revision.

config ANDROID_NDK_DEVEL_SUBDIR
    string "Subdirectory in the repository"
    default ""
    help
      Some projects produce releases not from the top-level directory in the
      repository, but rather from some subdirectory. If it is the case,
      specify this subdirectory here.

config ANDROID_NDK_DEVEL_BOOTSTRAP
    string "Bootstrap command"
    default ""
    help
      Command to run after checking out. Some projects don't store the generated
      files like configure script in the repository; building out of a checked out
      working copy thus requires some extra steps. Separate multiple shell commands
      with &&.

endif

config ANDROID_NDK_SRC_CUSTOM
    bool "Custom location"
    depends on EXPERIMENTAL
    help
      Custom directory or tarball.

if ANDROID_NDK_SRC_CUSTOM

config ANDROID_NDK_CUSTOM_LOCATION
    string "Custom source location"
    help
      Path to the directory or tarball with the sources.

endif

endchoice

choice
    bool "Version of android-ndk"
    help
      For a released version, select the version of android-ndk to download
      and build. For sources out of the vendor repository or from a custom
      location, select the version that describes those custom sources.
      Based on this version, crosstool-NG may apply some version-specific
      quirks while building android-ndk.

config ANDROID_NDK_VERY_NEW
    bool "newer than anything below"
    depends on EXPERIMENTAL
    depends on ANDROID_NDK_SRC_DEVEL || ANDROID_NDK_SRC_CUSTOM

config ANDROID_NDK_V_R16
    bool "r16"

config ANDROID_NDK_V_R15C
    bool "r15c"

config ANDROID_NDK_V_R14B
    bool "r14b"

config ANDROID_NDK_V_R13B
    bool "r13b (OBSOLETE)"
    depends on OBSOLETE

config ANDROID_NDK_V_R12B
    bool "r12b (OBSOLETE)"
    depends on OBSOLETE

config ANDROID_NDK_V_R11C
    bool "r11c (OBSOLETE)"
    depends on OBSOLETE

config ANDROID_NDK_V_R10E
    bool "r10e (OBSOLETE)"
    depends on OBSOLETE

config ANDROID_NDK_VERY_OLD
    bool "older than anything above"
    depends on OBSOLETE && EXPERIMENTAL
    depends on ANDROID_NDK_SRC_DEVEL || ANDROID_NDK_SRC_CUSTOM

endchoice


config ANDROID_NDK_VERSION
    string
    default "r16" if ANDROID_NDK_V_R16
    default "r15c" if ANDROID_NDK_V_R15C
    default "r14b" if ANDROID_NDK_V_R14B
    default "r13b" if ANDROID_NDK_V_R13B
    default "r12b" if ANDROID_NDK_V_R12B
    default "r11c" if ANDROID_NDK_V_R11C
    default "r10e" if ANDROID_NDK_V_R10E
    default "new" if ANDROID_NDK_VERY_NEW
    default "old" if ANDROID_NDK_VERY_OLD
    default "unknown"

config ANDROID_NDK_MIRRORS
    string
    default "https://dl.google.com/android/repository"

config ANDROID_NDK_ARCHIVE_FILENAME
    string
    default "@{pkg_name}-@{version}-linux-x86_64"

config ANDROID_NDK_ARCHIVE_DIRNAME
    string
    default "@{pkg_name}-@{version}"

config ANDROID_NDK_ARCHIVE_FORMATS
    string
    default ".zip"

config ANDROID_NDK_SIGNATURE_FORMAT
    string
    default ""




