# windows config options

## no-package
## depends on ARCH_X86 || (ARCH_ARM && ARCH_64)
## depends on EXPERIMENTAL
##
## select WINDOWS
##
## help Build a toolchain targeting systems running Windows as host

## select KERNEL_SUPPORTS_SHARED_LIBS
