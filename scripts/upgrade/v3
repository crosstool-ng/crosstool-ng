# vim: set filetype=sh :

# Upgrade v3 -> v4:
# - uClibc retired, leaving only uClibc-ng
upgrade()
{
    case "${opt}" in
        CT_LIBC_UCLIBC)
            replace CT_LIBC_UCLIBC_NG
            ;;
    esac
}
